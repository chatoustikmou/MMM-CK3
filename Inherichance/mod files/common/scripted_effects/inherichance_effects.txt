select_random_heir = {
	random_player_heir = {
		save_scope_as = random_heir
	}
	primary_heir = {
		save_temporary_scope_as = vanilla_heir
		set_variable = {
			name = heir
			value = scope:random_heir
		}
	}
	scope:random_heir = {
		every_heir_to_title = {
			limit = { holder = root }
			scope:vanilla_heir = {
				add_to_variable_list = {
					name = titles_inherited
					target = PREV
				}
			}
		}
	}
}